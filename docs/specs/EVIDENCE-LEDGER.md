# Evidence ledger

**Status:** **No experiment has ever run and no real footage has ever been processed. Every claim below is graded `assumed`.** The backend realignment and WO-123a frontend-operability correction pass on synthetic fixtures (legacy integration tests withheld to WO-134 remain deliberately red — see `handoff.md`), which establishes that the code works — not that any claim here is true.
**Governing:** `_oversight/DELIVERY-PLAYBOOK.md` honesty disciplines and [`docs/CONSTRAINTS.md`](../CONSTRAINTS.md). Reviewed at every ADP closeout — the milestone gates are retired (DECISIONS A-5c).

Grades: **measured** (a checkpoint/gate reported it) · **estimated** (derived from measured data) · **assumed** (believed, untested) · **refuted** (disproved).

A claim used in an ADR, an ES, or a pitch must be traceable to a row here. The pre-pivot ledger graded an autonomous-ranking programme that ADR-006 retired; those claims (old C-01…C-11, tied to EXP-001…009 and kill criteria) are **withdrawn with that programme** and are not carried forward.

## Load-bearing claims (post-pivot)

| # | Claim | Grade | Moved by | Notes |
|---|---|---|---|---|
| C-01 | Users value explained, approvable assistance enough to review a first draft rather than take one-tap automation | **assumed** | The deferred real-user check — the binding gate before the product is called *good* | **Pivotal to the pivot.** The whole product rests here. The archived CP-1 preference probe never ran, and its WO-100 prototype was deleted in the clean cut; no cheaper instrument is currently scheduled. |
| C-02 | Incumbent free tools leave a real "would I post this?" gap on a real day | **assumed** | The owner's competitive-floor comparison on a real day (deferred; formerly the archived CP-2) | Competitive landscape reads vendor docs, not measured output. Re-checked deliberately at each ADP closeout (absorption) — no milestone boundaries remain (A-5c). |
| C-03 | A legible, deterministic trim heuristic is a helpful starting point (kept more than discarded) | **assumed** | **`SPEC.md` §4.5 — the export summary line** (*"Kept 14 of 19 AI trims"*), derived from `disposition` at export | **Weakened then repaired, 2026-07-28.** (1) Until WO-116 the proposer read exposure off the *letterboxed* proxy, so **every landscape clip was labelled `OVEREXPOSED`** on every second — the claim was resting on a proposer that mislabelled an entire orientation, and no test caught it because the only exposure assertion used a black *portrait* clip. Fixed (`3d0d0d6`); two regression tests now guard it. (2) v3z retired `disposition`, removing the measure — but **DECISIONS A-3/A-3b reversed that**, keeping the field and naming its three writers. The measure exists again in `SPEC.md` §4.5. **(3) The instrument is complete end to end, 2026-07-30** — WO-118's `mark_proposals_accepted` writes the dispositions and returns the summary; WO-123a now calls it at export, appends the summary to §7.3's persistent `log.json`, and exposes the retained sidecar after reopen. **Built is not run: the grade cannot move until a real-footage run under ADP-004**, and synthetic fixtures cannot support this claim. **One bias to read the number with:** §4.5 taken literally makes a *binned* clip's untouched proposal `accepted`, because binning is §4.3's own control rather than the handle move §4.5 names — so the count is "proposals the user did not overrule", not "proposals that reached the export". Implemented as written; noted rather than corrected. |
| C-04 | Manual curation + AI trim yields a short reel **evaluable against — and plausibly as good as** — that day's Apple Memory | **assumed** | `SPEC.md` §12's validation gates on a real day's footage (ADP-004 / WO-135) | Evaluable and plausibly useful; superiority is a later real-user question, not claimed here. The Apple Memory comparison is named in WO-135's scope. |
| C-05 | A local web app + local FFmpeg meets the "one sitting" performance bar on the target Apple Silicon Mac | **assumed** | WO-135's performance run on a real ~50-clip day (ADP-004; carries forward the archived CP-3 spike) | If false, `SPEC.md` §10's promise needs revision. Playback alone is measured (WO-124, synthetic proxies — `SPEC.md` §6); ingest/analysis/render on real footage are not. |
| C-06 | Centre-crop to 9:16 is acceptable on real, people-centred footage | **assumed** | `SPEC.md` §12 on a real day's footage (ADP-004 / WO-135) | Saliency reframing is deferred (`SPEC.md` §13); centre-crop is the fallback and can decapitate people at frame edges. |
| C-07 | ~~A legible selection/ordering heuristic makes a trusted first pass (review beats ordering from scratch)~~ | **withdrawn 2026-07-29** | Nothing — the assist it describes was **cancelled permanently** (DECISIONS A-5b) | The machine never proposes which clips belong in the reel, so the claim has no subject left in the product. Withdrawn rather than regraded, like the pre-pivot set; `PROJECT.md` records it as a retired assumption. |
| C-08 | Rules-based speed ramps (motion energy + audio level, `SPEC.md` §4.2) read as intentional rather than cheap | **assumed** | The §4.5 kept-count at export, read on a real-footage run (ADP-004) | Least-certain signal; **built last so the net-negative de-scope trigger can fire on it cheaply** (A-5a). Beat alignment is deferred and no longer part of this claim (`SPEC.md` §13). "A wrong ramp reads as kitsch instantly." |
| C-09 | The pre-value workflow (iPhone→Mac folder + user-supplied rights-cleared track) is tolerable to a real user | **assumed** | deferred real-user validation | Tolerable to the owner now; a named real-user adoption risk (risk register). |
| C-10 | Someone would pay for this | **assumed** | deferred | Weakest claim. 2–6 uses/year is hostile to subscriptions. |
| C-11 | Apple/Google will not absorb this *transparent, reversible* flow during the project's life | **assumed** | Re-checked deliberately at each ADP closeout — no milestone boundaries remain (A-5c) | Decays; cannot be settled once. More platform *automation* is not the trigger. ("Staged" dropped from the claim: the staged flow itself was retired.) |

## Implementation reality

Separate from claims, per the playbook's *committed is not shipped* discipline.

| Item | State |
|---|---|
| Product code | **Backend realigned to schema v2; ADP-002 closed 2026-07-30; WO-123a and WO-125 – WO-127 merged locally.** Contracts (WO-117), ingest (WO-116a), trim proposer (WO-118a), store (WO-118/118b), media services (WO-119), speed proposer (WO-120), renderer (WO-121), output QA (WO-122), API core (WO-123) and the corrected frontend-operability seam (WO-123a) are complete. The server actions, persistent Log wiring, music probe, root-confined link repair and `Referer` guard pass their focused and synthetic flow gates. The whole suite exposes **168 pass, 3 fail, 1 import error and 1 owner-gated skip**; only legacy integration tests owned by withheld WO-134 remain red (`handoff.md` has the detail). **Frontend: rack foundation, shared app kernel and Monitor/Transport** — WO-125's typed v3z system, WO-126's six-state shell/client plus Amendment 2's session-only preview queue, and WO-127's dual-video media-event playback and fixed transport pass all 45 frontend contract tests, typecheck, build and six-state browser smoke. Five product modules remain placeholders. |
| Media / corpus / consent records / annotations | **none** |
| Experiment / checkpoint results | **none** — no checkpoint has run. Every claim above is `assumed`, and nothing has been exercised against real footage. (WO-124 measured playback behaviour on synthetic proxies — an engineering measurement recorded in `SPEC.md` §6, not an evidence checkpoint; it moves nothing here.) |
| Governing decisions | The 2026-07-28 clean cut archived the thirteen ADRs (non-citable); the surviving guardrails are in [`docs/CONSTRAINTS.md`](../CONSTRAINTS.md). **The v3z departures are decided** — [`DECISIONS.md`](../DECISIONS.md), owner, 2026-07-28 — and **`SPEC.md` is accepted** (2026-07-28), its §14 fully closed as of 2026-07-29. DECISIONS §5 (2026-07-30) narrowly amends the HTTP seam so the frozen frontend can operate the frozen product without patching internal state. ADP-002 closed 2026-07-30 after seven amendments; Amendment 7's WO-124 retention was fulfilled by WO-127's foreground rerun and deletion. ADP-003 was authorized 2026-07-30 and amended twice: Amendment 1 added WO-123a before WO-125 and Amendment 2 repaired WO-126's typed, non-persisted preview-queue seam. Local mock/synthetic implementation only. |
| Git | Public remote `origin` → `github.com/vasudevap/KwikReel` (**PUBLIC**). Local `main` is ahead of `origin/main`; each push is a separate owner decision. |

## Log

| Date | Change |
|---|---|
| 2026-07-20 | Ledger initialized alongside the (now retired) Stage B validation plan. All claims graded `assumed`. |
| 2026-07-23 | Pivot to a human-directed, approval-gated editor (ADR-005/006/007/008). The autonomous-ranking claim set (old C-01…C-11 / EXP-001…009) is withdrawn with the programme. No media collected. |
| 2026-07-24 | Pre-ADP course correction (ADR-009–013). Ledger reset to the post-pivot load-bearing claims above; all `assumed`. Prior "no remote" note corrected — a public remote exists. PROP-01 participation was withdrawn by ADR-006, so no pilot-launch entry stands. No media collected; no experiment run. |
| 2026-07-28 | **Clean cut** ahead of the v3z rebuild: superseded documents archived and made non-citable, guardrails consolidated into `docs/CONSTRAINTS.md`, obsolete frontend deleted. **C-03 weakened** on two independent grounds (see its note) after the letterbox defect was found and fixed. `Product code` row corrected — it read **none** while a complete backend existed. No media collected; no experiment run. |
| 2026-07-28 | **`DECISIONS.md` decided, `SPEC.md` accepted, ADP-002 authorized** (amended same day: WO-118a and WO-116a added, WO-120 unheld). WO-117 froze schema v2; WO-116a gave proxies audio; WO-118a rebuilt the trim proposer to A-6; WO-124 measured the playback engine. No media collected; no experiment run; no grade moved — ADP-002 §7 forbids it on synthetic fixtures. |
| 2026-07-29 | `SPEC.md` §14 fully closed (SO-2 – SO-4; SO-1 closed 2026-07-28). **Ledger reconciled to the decided record:** the `Governing decisions` row still called the v3z departures *undecided* — corrected; instruments naming retired milestone gates (C-01, C-02, C-04, C-05, C-06, C-11) repointed at their live equivalents; **C-07 withdrawn** — its assist was cancelled permanently (A-5b); C-08 reworded to the beat-free `SPEC.md` §4.2 assist. Every grade stays `assumed`. |
| 2026-07-29 | **WO-118 merged: C-03's instrument is built.** `mark_proposals_accepted` writes §4.5's dispositions at export and returns the *"Kept 14 of 19 AI trims"* line; §7.3's `log.json` persists it, so the measure that had nowhere to land now has somewhere. **No grade moves** — ADP-002 §7 forbids it on synthetic fixtures, and only ADP-004's real-footage run can. C-03's note gains the binned-clip bias in the count, and a stop-and-ask stands on §4.5's third writer (`dismissed`), which is unimplemented because §4.3 and §3.1 contradict each other. No media collected; no experiment run. |
| 2026-07-30 | **ADP-002 Amendment 5 recorded.** Its executable manifest now names production and test paths, WO-121 → WO-122 is one serial renderer/QA lane, and WO-118b is a held closeout gate for the unresolved reject semantics. This changes authorization and sequencing, not implementation or evidence: no code ran, no media was collected, no experiment ran, and no grade moved. |
| 2026-07-30 | **Amendment 6 and two implementation lanes reconciled.** WO-118b now retains dismissed trim proposals while deriving the whole/user trim, and WO-120 proposes deterministic source-time speed ranges. The synthetic suite is 123 pass, 0 fail, 5 expected v1-shape collection errors. No media was collected, no experiment ran, and no grade moved. |
| 2026-07-30 | **WO-119 merged: local preview-media services are v2.** Thumbnails and peaks cache outside source folders, music peaks are keyed by content hash before a project exists, and native file selection is cancellable. The synthetic suite is 129 pass, 0 fail, 5 expected v1-shape collection errors. No media was collected, no experiment ran, and no grade moved. |
| 2026-07-30 | **WO-121 merged: the renderer is v2.** One local export now reads the derived timeline, applies arithmetic speed clamps with chained audio tempo, uses the project resolution and weighted audio levels, skips out-of-reel clips, refuses upscaling, and strips source metadata. The synthetic suite is 137 pass, 0 fail, 4 expected v1-shape collection errors. No real media was processed, no experiment ran, and no grade moved. |
| 2026-07-30 | **WO-122 merged: output QA is v2.** It checks each render against the derived duration, selected resolution and level-based audio contract, and reports a reason for bad exports. The synthetic suite is 142 pass, 0 fail, 4 expected v1-shape collection errors. No real media was processed, no experiment ran, and no grade moved. |
| 2026-07-30 | **WO-123 merged: the API is v2.** It exposes the frozen PATCH, relink, media and single-export surface, removes approval/finalize/audio-mode routes, and keeps every mutation capability-protected. The synthetic suite exposes 156 pass, 3 legacy integration failures and 1 legacy integration import error; those tests belong to withheld WO-134, while every ADP-002 gate passes. No real media was processed, no experiment ran, and no grade moved. |
| 2026-07-30 | **ADP-002 closed and ADP-003 drafted, not authorized.** All eleven ADP-002 Work Orders passed their own synthetic gates. Amendment 7 resolves the closeout/SPEC conflict by retaining the WO-124 harness only through WO-127, which owns the required foreground rerun, result append and deletion. This is a program-state change, not evidence: no real media was processed, no experiment ran, and no grade moved. |
| 2026-07-30 | **ADP-003 authorized as drafted.** WO-125 – WO-132 may now build and test locally on mock/synthetic state in the program's dependency order. Push, CI, real media, new dependencies and contract/design changes remain withheld. Authorization is not implementation or evidence: WO-125 has not begun, no media was processed, no experiment ran, and no grade moved. |
| 2026-07-30 | **Frontend-operability contract correction recorded; ADP-003 Amendment 1 authorized.** A review found that reject, reversible binning, the persistent Log, server-computed music metadata and automatic link repair had no complete frontend-to-server path, and the binding `Referer` guard lacked implementation. DECISIONS §5 makes them explicit server-owned actions, amended `SPEC.md` §8 names the narrow routes, and unheld WO-123a now runs before WO-125. This is authorization and specification, not implementation or evidence: the suite has not changed, no media was processed, no experiment ran, and no grade moved. |
| 2026-07-30 | **WO-123a merged: the corrected frontend-operability seam is executable.** Server-owned bin/restore and trim reject, persistent server/client Log paths, pre-project music probe, root-confined content-hash repair and the `Referer` guard pass focused gates plus a synthetic create→scan→analyze→propose→control→export→Log flow. C-03's export summary now reaches persistent history and survives reopen. The suite is 168 pass, 3 legacy failures, 1 legacy import error and 1 owner-gated skip. No real media was processed, no experiment ran, and no grade moved. |
| 2026-07-30 | **WO-125 merged: the frontend rack foundation is executable.** Typed props render v3z modules, rack hardware, physical keys and state signals, display glass, locked housings and CSS-drawn glyphs using embedded fonts. The fixed 960 px geometry, 465 px monitor column and 26 px key cause are enforced in code and browser smoke; 13 contract tests, typecheck and build pass. This is implementation, not evidence: no media was processed, no experiment ran, and no grade moved. |
| 2026-07-30 | **WO-126 merged: the shared frontend app kernel is executable.** The six accepted states render as one fixed view through typed module slots with honest placeholders; mock and live clients share one interface; ordered optimistic writes preserve later intent and visibly revert/report 409 conflicts. Seventeen focused tests (30 frontend tests combined), typecheck, build and six-state browser smoke pass. This is implementation, not evidence: no media was processed, no experiment ran, and no grade moved. |
| 2026-07-31 | **ADP-003 Amendment 2 and WO-127 merged: Monitor and Transport are executable.** The narrow WO-126 reopening adds §3.5's session-only preview queue without persisting it. WO-127 implements two-slot cross-swapped preview, media-event timing, clip-scoped scrub, target length, resolution and Loop. The required visible, focused WO-124 rerun measured a 25.8 ms median prepared presentation gap against 58.3 ms for one reloaded element, zero seek frame error, exact preview rates and music drift within 3.4 ms across a cut; the retained harness and generated fixtures were then deleted. Forty-five frontend tests, typecheck, build and six-state browser smoke pass. This is implementation QA and an engineering measurement, not product evidence: no real media was processed, no experiment ran, and no grade moved. |
