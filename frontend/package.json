{
  "name": "kwikreel-frontend",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "KwikReel frontend. WO-117 owns this manifest; deps stay within the declared set (react, typescript, vite). src/types/contracts.ts is generated from the backend models — do not hand-edit. No frontend is authorized yet: ADP-003 waits on WO-124's measurements.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.3.4"
  }
}
